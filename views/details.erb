<p>Your will be flying <%= session[:airline] %> flight number <%= session[:flight_number] %> departing from <%= session[:origin] %> and heading to <%= session[:destination] %>, Departing at <%= session[:time] %></p>

<form method="post" action="/flights/add">
    <div>
      <label for="flight_name">Choose a name for your journey:
        <input name="flight_name" id="flight_name" value=""/>
      </label>
    </div>
    <button class="inline" type="submit">Save Flight</button>
</form>

<a href="/">New Flight</a>
